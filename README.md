## Getting started

Change the DATABASE_URL to a valid database url in .env file

Start the server

```sh
cd server
denon start
```

Make sure database is connected

Start frontend

```sh
cd web
yarn install
yarn dev
```
